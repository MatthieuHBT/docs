---
title: Modifier un budget
api: PATCH https://roapix.up.railway.app/api/v1/budgets/{campaignId}
description: Modifie le budget journalier d'une campagne Meta.
---

<ParamField header="Authorization" type="string" required>
  `Bearer roapix_votre_clé`
</ParamField>

<ParamField path="campaignId" type="string" required>
  ID Meta de la campagne (ex : `120213456789`)
</ParamField>

<ParamField body="daily_budget" type="number" required>
  Nouveau budget journalier en USD
</ParamField>

<RequestExample>
```bash cURL
curl -X PATCH "https://roapix.up.railway.app/api/v1/budgets/120213456789" \
  -H "Authorization: Bearer roapix_votre_clé" \
  -H "Content-Type: application/json" \
  -d '{"daily_budget": 350.00}'
```

```python Python
import requests

r = requests.patch(
    "https://roapix.up.railway.app/api/v1/budgets/120213456789",
    json={"daily_budget": 350.00},
    headers={"Authorization": "Bearer roapix_votre_clé"}
)
print(r.json()["data"])
```
</RequestExample>

<ResponseExample>
```json 200
{
  "data": {
    "campaign_id": "120213456789",
    "campaign_name": "Felipet Cat — FR — Acquisition",
    "daily_budget": 350.00,
    "effective_status": "ACTIVE",
    "updated_at": "2026-03-23T20:00:00.000Z"
  },
  "workspace_id": "a1b2c3d4-...",
  "generated_at": "2026-03-23T20:00:00.000Z"
}
```
</ResponseExample>
